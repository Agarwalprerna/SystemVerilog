module tb;
  integer Var;  //range -2**31 to 2**31
  
  initial begin
    for(int i =0 ; i<7 ; i++) begin
      if(randomize(Var))
        $display("done: var= %0d", Var);
      else
        $display("failed randomization");
    end
    
  end
endmodule
